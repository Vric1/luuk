# 🚀 Деплой бота на Railway (бесплатно)

Railway предоставляет **$5 бесплатно каждый месяц** — этого хватит для работы бота 24/7.

## Подготовка

1. **Убедись, что токены обновлены:**
   - В `@BotFather` создай новый токен для бота
   - В OpenRouter создай новый API ключ

## Вариант 1: Railway (рекомендуется)

### Шаг 1: Создай аккаунт
1. Иди на https://railway.app/
2. Войди через **GitHub**
3. Подтверди email

### Шаг 2: Загрузи код
1. Создай новый репозиторий на GitHub
2. Загрузи все файлы из папки `telegram-rp-bot`
3. **НЕ ВКЛЮЧАЙ** `config.py` в репозиторий (он в `.gitignore`)

### Шаг 3: Деплой
1. В Railway нажми **"New Project"**
2. Выбери **"Deploy from GitHub repo"**
3. Выбери свой репозиторий с ботом
4. Railway автоматически определит Python и установит зависимости

### Шаг 4: Настрой переменные окружения
1. В проекте Railway иди в **"Variables"**
2. Добавь переменные:
   - `TELEGRAM_BOT_TOKEN` = твой токен бота
   - `OPENROUTER_API_KEY` = твой OpenRouter ключ
   - `MODEL_NAME` = `tngtech/deepseek-r1t2-chimera:free`

### Шаг 5: Запуск
1. Railway автоматически задеплоит бота
2. В логах увидишь "🤖 Бот с ИИ запущен!"
3. Готово! Бот работает 24/7

---

## Вариант 2: Render

### Шаг 1: Регистрация
1. Иди на https://render.com/
2. Войди через GitHub

### Шаг 2: Создай Web Service
1. Нажми **"New Web Service"**
2. Подключи свой GitHub репозиторий
3. Настройки:
   - **Name**: `telegram-rp-bot`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python bot.py`

### Шаг 3: Переменные окружения
В разделе **Environment**:
- `TELEGRAM_BOT_TOKEN` = твой токен
- `OPENROUTER_API_KEY` = твой ключ
- `MODEL_NAME` = `tngtech/deepseek-r1t2-chimera:free`

### Шаг 4: Деплой
Нажми **"Create Web Service"** и жди деплоя.

---

## Вариант 3: Heroku (ограниченно)

⚠️ **Heroku теперь платный**, но можно использовать с картой.

1. Создай аккаунт на heroku.com
2. Установи Heroku CLI
3. В терминале:
```bash
heroku create your-bot-name
heroku config:set TELEGRAM_BOT_TOKEN=your_token
heroku config:set OPENROUTER_API_KEY=your_key
heroku config:set MODEL_NAME=tngtech/deepseek-r1t2-chimera:free
git push heroku main
```

---

## Вариант 4: Replit

1. Иди на https://replit.com/
2. Создай новый **Python Repl**
3. Загрузи все файлы бота
4. В **Secrets** (замочек слева) добавь:
   - `TELEGRAM_BOT_TOKEN`
   - `OPENROUTER_API_KEY`
   - `MODEL_NAME`
5. Нажми **Run**

---

## Проверка работы

После деплоя:
1. Найди своего бота в Telegram
2. Напиши `/start`
3. Протестируй команды:
   - `/ai Привет!`
   - `/file Напиши стих`
   - `/hug` (с ответом на сообщение)

## Мониторинг

- **Railway**: смотри логи во вкладке "Deployments"
- **Render**: логи в разделе "Logs"
- **Heroku**: `heroku logs --tail`

## Если что-то не работает

1. Проверь логи хостинга
2. Убедись, что все переменные окружения указаны
3. Проверь, что токены рабочие
4. Убедись, что `requirements.txt` содержит все зависимости

## 💰 Лимиты бесплатных хостингов

- **Railway**: $5/месяц (хватает на полную работу)
- **Render**: 750 часов/месяц (достаточно)
- **Replit**: Всегда бесплатно, но может засыпать

**Рекомендация**: Используй Railway — самый надёжный и простой вариант.