# 🚀 Деплой на Scalingo

Scalingo — европейская платформа PaaS с **бесплатным тарифом** для небольших проектов.

## 💰 Бесплатный план
- **512MB RAM**
- **1 контейнер** 
- **Ограниченное время работы** (засыпает при неактивности)
- **Достаточно для Telegram бота**

## 📋 Подготовка

### 1. Создание аккаунта
1. Иди на https://scalingo.com/
2. Нажми **"Sign up"**
3. Выбери регион: **osc-fr1** (Франция) — бесплатный
4. Подтверди email

### 2. Установка CLI (опционально)
```bash
# Для Windows (через PowerShell)
iwr -useb https://cli-dl.scalingo.com/install | iex

# Для Linux/macOS
curl -O https://cli-dl.scalingo.com/install && bash install
```

## 🔧 Деплой через Web-интерфейс

### Способ 1: Через GitHub

1. **Загрузи код на GitHub:**
   - Создай новый репозиторий
   - Загрузи все файлы из папки `telegram-rp-bot`
   - **ВАЖНО**: не включай `config.py` (он в .gitignore)

2. **Создай приложение в Scalingo:**
   - Войди в https://dashboard.scalingo.com/
   - Нажми **"Create new app"**
   - **Name**: `telegram-rp-bot-[твое-имя]`
   - **Region**: osc-fr1 (бесплатный)

3. **Подключи GitHub:**
   - В настройках приложения: **"Deploy" → "Link with GitHub"**
   - Выбери свой репозиторий
   - **Branch**: `main` или `master`
   - Включи **"Auto deploy"**

### Способ 2: Через Git напрямую

```bash
# В папке с ботом
git init
git add .
git commit -m "Initial commit"

# Добавь Scalingo remote
scalingo create telegram-rp-bot-[твое-имя]

# Деплой
git push scalingo main
```

## ⚙️ Настройка переменных окружения

В Dashboard Scalingo:
1. Иди в **"Environment"**
2. Добавь переменные:

| Переменная | Значение | Обязательно |
|------------|----------|-------------|
| `TELEGRAM_BOT_TOKEN` | Токен от @BotFather | ✅ |
| `OPENROUTER_API_KEY` | API ключ от OpenRouter | ✅ |
| `MODEL_NAME` | `tngtech/deepseek-r1t2-chimera:free` | ❌ |
| `PYTHONUNBUFFERED` | `1` | ❌ |

## 🚀 Процесс деплоя

1. После настройки переменных нажми **"Deploy"**
2. Scalingo автоматически:
   - Определит Python приложение
   - Установит зависимости из `requirements.txt`
   - Запустит бота через `Procfile`
3. В логах увидишь: `🤖 Бот с ИИ запущен!`

## 📊 Мониторинг

### Логи
```bash
# Через CLI
scalingo -a telegram-rp-bot-[имя] logs

# Или в Dashboard: "Activity" → "Logs"
```

### Статистика
- **Dashboard** → **"Analytics"**
- Использование памяти, CPU
- Количество запросов

## 🔧 Масштабирование

### Бесплатный план
```bash
# Проверить статус
scalingo -a telegram-rp-bot-[имя] ps

# Рестарт (если нужно)
scalingo -a telegram-rp-bot-[имя] restart
```

### Платные планы (от €7/мес)
- Больше памяти (1GB+)
- 24/7 работа без засыпания
- Несколько контейнеров
- База данных

## ⚠️ Ограничения бесплатного плана

1. **Засыпание**: бот засыпает через 30 минут неактивности
2. **Пробуждение**: просыпается при первом запросе (~10-30 секунд)
3. **Лимиты**: 512MB RAM, ограниченное время работы
4. **Решение**: для 24/7 работы нужен платный план

## 🐛 Решение проблем

### Бот не отвечает
1. Проверь логи: `scalingo logs`
2. Убедись, что переменные окружения заданы
3. Проверь, что бот не заснул (отправь `/start`)

### Ошибка деплоя
1. Проверь `requirements.txt`
2. Убедись, что `Procfile` содержит: `web: python bot.py`
3. Проверь синтаксис Python

### Конфликт портов
Scalingo автоматически назначает порт через `$PORT`, но для Telegram бота это не нужно.

## 📱 Deploy Button

Можешь создать кнопку быстрого деплоя:

```markdown
[![Deploy](https://my.scalingo.com/deploy/button.svg)](https://my.scalingo.com/deploy?source=https://github.com/your-username/telegram-rp-bot)
```

## 🔄 Автоматические деплои

При коммите в GitHub → автоматический деплой в Scalingo:
1. **Settings** → **"Integrations"** → **"GitHub"**
2. Выбери репозиторий и ветку
3. Включи **"Auto deploy"**

## 💡 Советы

1. **Новые токены**: пересоздай токены после деплоя
2. **Логи**: регулярно проверяй на ошибки
3. **Мониторинг**: настрой уведомления о падении
4. **Backup**: храни код в Git
5. **Testing**: тестируй локально перед деплоем

---

## 🚀 Быстрый старт

1. Создай аккаунт на Scalingo
2. Загрузи код на GitHub  
3. Создай новое приложение в Scalingo
4. Подключи GitHub репозиторий
5. Настрой переменные окружения
6. Нажми **Deploy**
7. Готово! 🎉

**Время деплоя: ~5 минут**