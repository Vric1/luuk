#!/usr/bin/env python3
"""
Стартовый скрипт для деплоя бота
"""

import os
import sys

# Добавляем текущую директорию в путь
sys.path.append(os.path.dirname(os.path.abspath(__file__)))

# Проверяем наличие необходимых переменных окружения
required_vars = ["TELEGRAM_BOT_TOKEN"]

missing_vars = [var for var in required_vars if not os.getenv(var)]

if missing_vars:
    print("❌ Отсутствуют обязательные переменные окружения:")
    for var in missing_vars:
        print(f"   - {var}")
    print("\n💡 Укажи переменные в настройках хостинга или создай .env файл")
    sys.exit(1)

print("✅ Все переменные окружения найдены")
print("🚀 Запуск бота...")

# Импортируем и запускаем основной файл бота
if __name__ == "__main__":
    import bot